import { defineConfig, devices } from '@playwright/test';

/**
 * E2E smoke tests. The dev server is started automatically (reused locally,
 * fresh in CI). These guard the "white screen" class of regressions — a render
 * throw reaching the ErrorBoundary — which unit tests can't observe.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
